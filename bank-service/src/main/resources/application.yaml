server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bankdb
    username: bankuser
    password: bankpass

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: bank
    show-sql: true
